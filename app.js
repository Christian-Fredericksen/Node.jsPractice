// another useful module is a path module that allows us to easily interact with the file path.

const path = require('path')  //<---like before, we set a var to whatever module. in this case
                              // the 'path' module