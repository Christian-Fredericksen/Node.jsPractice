// we set a couple of vars.
const num1 = 5;
const num2 = 10;
// we write a function
function addNums() {
    console.log(`the sum is: ${num1 + num2}`);
}

addNums()  // then we invoke this method within this module. Head back to '3-modules.js'